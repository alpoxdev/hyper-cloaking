{
  "name": "hyper-cloaking",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "mcp"
  ],
  "description": "Portable Hyper Cloaking skill for CloakBrowser-backed Playwright MCP workflows.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "jsdoc:check": "node scripts/audit-jsdoc.mjs check all",
    "mcp:jsdoc:check": "node scripts/audit-jsdoc.mjs check all",
    "skills:list": "npx skills add . --list",
    "lint": "oxlint plugins scripts tests mcp/src/**/*.mjs mcp/engine/**/*.mjs mcp/test/**/*.mjs mcp/*.mjs",
    "lint:fix": "oxlint --fix plugins scripts tests mcp/src/**/*.mjs mcp/engine/**/*.mjs mcp/test/**/*.mjs mcp/*.mjs",
    "test": "node --test tests/e2e/*.test.mjs && cd tests/unit/engine && node --test",
    "engine:validate": "node mcp/engine/cli.mjs validate --json",
    "mcp:test": "node --test --test-concurrency=1 --test-timeout=120000 mcp/test/*.test.mjs",
    "mcp:publish": "npm --workspace mcp publish --access public",
    "build": "npm --workspace mcp run build",
    "ci": "npm run validate && npm run lint && npm run format:check && npm test && npm run engine:validate && npm run mcp:test",
    "ci:compat": "npm test && npm run engine:validate && npm run mcp:test",
    "format": "prettier --write \"scripts/**/*.mjs\" \"tests/**/*.mjs\" \"mcp/src/**/*.mjs\" \"mcp/test/**/*.mjs\" \"mcp/*.mjs\" \".github/workflows/ci.yml\"",
    "format:check": "prettier --check \"scripts/**/*.mjs\" \"tests/**/*.mjs\" \"mcp/src/**/*.mjs\" \"mcp/test/**/*.mjs\" \"mcp/*.mjs\" \".github/workflows/ci.yml\""
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "cloakbrowser": "^0.4.10",
    "playwright-core": "^1.61.1"
  },
  "devDependencies": {
    "acorn": "8.15.0",
    "oxlint": "^1.73.0",
    "prettier": "^3.9.5"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
